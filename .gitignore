# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment — secrets. Never commit.
.env
.env.*
!.env.example

# Local uploads (media is served through an authorized route, not from git)
uploads/

# Prisma client is generated output — rebuild with `npm run db:generate`.
# Committing it creates merge conflicts and drifts from the schema.
**/src/generated/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
